from gturtle import *
joe=Turtle()
a=(5,6) # a est un tuple non modifiable a[0]=5 et a[1]=6
a=[5,6] # a est une liste 
# Entrées clavier
a=input("Entrer un texte:")
a=int(input("Entrer un entier:"))
a=float(input("Entrer un réel"))

# Impression écran
print("bonjour")
print(variable)
print("la valeur est ",variable)
print("la valeur est "+str(variable))

# les for for for for for for for for for for for for for for for for
for i in range (5): # i prend les valeurs 0 1 2 3 4
    # instructions à compléter

for i in range (3,7): # i prend les valeurs 3 4 5 6
    # instructions à compléter

for i in range (0,5,2): # i prend les valeurs 0 2 4
    # instructions à compléter
    

# les while while while while while while while while while while while while
while (i<5):
    # instructions à compléter
    
    
# les if if if if if if if if if if if if if if if if if if if if if if
if ( i<5  ):
    # instructions à compléter

if ( i<5  ):
    # instructions à compléter
else :
    # instructions à compléter
	
if ( i<5  ):
    # instructions à compléter
elif (i>10) :
    # instructions à compléter
else:
   # instructions à compléter
   
   
def nomdelafonction():# fonction sans paramètre ne renvoyant pas de résultat
     # instructions à compléter

def nomdelafonction(par1):# fonction avec 1 paramètre ne renvoyant pas de résultat
     # instructions à compléter

def nomdelafonction(par1, par2):# fonction avec 2 paramètres ne renvoyant pas de résultat
     # instructions à compléter

def nomdelafonction():# fonction sans paramètre  renvoyant un résultat
     # instructions à compléter
    return resultat

def nomdelafonction():# fonction sans paramètre  renvoyant 2 résultats
     # instructions à compléter
    return resultat1, resultat2

def nomdelafonction(par1):# fonction avec1 paramètre  renvoyant un résultat
     # instructions à compléter
    return resultat

def nomdelafonction(par1,par2):# fonction avec 2 paramètres  renvoyant 2 résultats
     # instructions à compléter
    return resultat1, resultat2     
 

